<nav>
    <div class="menu">
        <% if @current_user.present? %>
            <div><%= link_to "My Journals", journals_path %></div>
            <div><%= link_to "My Plants", plants_path %></div>
            <div><%= link_to "Log out #{ @current_user.nickname }", login_path, :method => :delete %></div>
        <% else %>
            <div><%= link_to 'Home', root_path %></div>
            <div><%= link_to 'Sign Up!', new_user_path %></div>
        <% end %>
    </div>
</nav>