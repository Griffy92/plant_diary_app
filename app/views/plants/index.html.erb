<!-- Shows all plants that belong to the user -->

<!-- Sub Nav -->
<div class="nav-control">
    <div class="nav-control-container">
        <% if @current_user.present? %>
            <li class="menu-item">
                <%= link_to 'New Plant', new_plant_path, :class => 'nav-control-link' %>
            </li>
        <% end %>
    </div>
</div>

<!-- Content -->
<!-- 
<div class="container text-center">
    <div class="row">
        <div class="col-12">
            <div class="d-flex align-items-center flex-wrap">
                <% @plants.uniq.each do |plant| %> 
                        <figure class="figure">
                            <% if plant.image.present? %>
                                <%= cl_image_tag plant.image, :class => 'img-thumb' %>
                            <% end %>
                            <figcaption class="figure-caption text-center"><%= link_to plant.name, plant %></figcaption>
                        </figure>
                <% end %>
            </div>
        </div>
    </div>
</div>
-->



<div class="grid-container">
    <div class="grid-item picture-1">
        <h2>Woody</h2>
    </div>
    <% @plants.uniq.each do |plant| %>
    <div class="grid-item">
        <%= link_to cl_image_tag(plant.image, :class => 'picture-2'), plant %>
        <%# <img src="https://i.imgur.com/Xi6iFon.jpg" alt="" class="picture-2"> %>
    </div>
    <% end %>
</div>